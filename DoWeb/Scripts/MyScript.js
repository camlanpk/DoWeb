//$(function () { //        $("#TenMayKhuon").autocomplete({ //            source: '@Url.Action("AutocompleteMayKhuon", "KHUONEP")', //            minLength: 1, //            select: function (event, ui) { //                $("#TenMayKhuon").val(ui.item.label); //                $("#MaMayKhuon").val(ui.item.value); //                return false; //            } //        }); //    });     //function tinhXepLoai(tb) {    //     tb = parseFloat(tb);    //     let xeploai = "";     //     if (tb >= 4.5) xeploai = "Giỏi";    //     else if (tb >= 3.5) xeploai = "Khá";    //     else if (tb >= 2.5) xeploai = "Trung bình";    //     else if (tb >= 1.5) xeploai = "Không đạt";    //     else xeploai = "Kém";     //     $("#XepLoai").val(xeploai);    // }    function openPopup(url) {         $("#formModal .modal-content").load(url, function () {             $("#formModal").modal("show");         });     }     function closePopup() {        $("#formModal").modal("hide");     function openPopup(url) {             $("#formModal .modal-content").load(url, function () {                 $("#formModal").modal("show");             });         }          $(document).on("submit", "form", function (e) {             e.preventDefault();             var form = $(this);             $.ajax({                 type: form.attr("method"),                 url: form.attr("action"),                 data: form.serialize(),                 success: function (res) {                     if (res.success) {                         location.reload();                     } else {                         $("#formModal .modal-content").html(res);                     }                 }             });         });      $(document).on("submit", "form", function (e) {         e.preventDefault();         var form = $(this);         $.ajax({             type: form.attr("method"),             url: form.attr("action"),             data: form.serialize(),             success: function (res) {                 if (res.success) {                     location.reload();                 } else {                     $("#formModal .modal-content").html(res);                 }             }         });     })
}